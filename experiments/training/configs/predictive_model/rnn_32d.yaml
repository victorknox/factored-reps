name: rnn
instance:
  _target_: experiments.models.rnn.RNN
  cfg:
    _target_: experiments.models.rnn.RNNConfig
    d_vocab: ???
    d_model: 32
    n_layers: 2
    rnn_type: rnn
    dropout: 0.0
    device: ${device}
    seed: ${weight_init_seed}
    n_ctx: 8
load_checkpoint_step: null
